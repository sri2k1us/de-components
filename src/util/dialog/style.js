import Color from "../CyVersePalette";

export default (theme) => ({
    dialogCloseButton: {
        position: "absolute",
        right: 0,
        top: 5,
        color: Color.white,
    },
    header: {
        backgroundColor: Color.darkBlue,
    },
    title: {
        color: Color.white,
    },
});
